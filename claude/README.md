# ACT Prep System

See the [full documentation and setup guide](../README.md) at the repo root.

Quick commands from inside this folder:

```bash
cp student-config.example.md student-config.md   # first-time setup
claude                                             # launch Claude Code
```

Then type `Start English`, `Start Reading`, `Start Science`, or `Start Math` to begin.
